name: cxhero
description: Lightweight event tracking for Flutter with per-session and optional user-id scoping, persisted as JSON on disk, plus configurable micro-surveys.
version: 0.1.0
homepage: https://github.com/indigothm/cxhero-flutter

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.3.0
  http: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
